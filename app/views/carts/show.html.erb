<% if notice %>
<p id="notice"><%= notice %></p>
<% end%>

<h2>Pragmaticカート</h2>
<ul>
  <% @cart.line_items.each do |item| %>
    <li><%= item.product.title %></li>
  <% end %>
</ul>

<%= link_to ('買い物ば続ける'), store_path %>
